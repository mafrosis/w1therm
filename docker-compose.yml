version: '3'

services:
  fridge:
    image: mafrosis/fridge
    restart: always
    privileged: true
    build:
      context: .
    environment:
      TARGET_TEMP: 13
    volumes:
      - ./fridge.py:/srv/app/fridge.py
